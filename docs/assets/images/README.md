# Images

Store documentation images in this directory.

Reference images from Markdown files with relative links. For example, a page in `docs/user-guide/` can refer to an image here as:

```md
![Short alt text](../assets/images/example.png)
```

Use descriptive filenames and short alt text so the source remains easy to scan.
