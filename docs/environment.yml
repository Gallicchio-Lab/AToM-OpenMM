name: atom-openmm-docs
channels:
  - conda-forge
dependencies:
  - python=3.12
  # Keep the current MkDocs/Material stack until the MkDocs 2.x ecosystem settles.
  - "mkdocs>=1.6,<2"
  - "mkdocs-material>=9.5,<10"
