# Reference

This section is reserved for stable reference material such as command-line entry points, configuration keys, file formats, and output conventions.

API reference generation is intentionally not enabled yet. The documentation currently uses normal Markdown files only.
